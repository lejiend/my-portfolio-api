services:
  api:
    build: .
    container_name: my-portfolio-api
    restart: unless-stopped
    ports:
      - '5001:5001'
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
